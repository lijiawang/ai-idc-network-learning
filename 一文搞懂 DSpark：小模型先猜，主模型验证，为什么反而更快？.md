# 一文搞懂 DSpark：小模型先猜，主模型验证，为什么反而更快？

> **一句话理解：DSpark 让草稿模型先提出候选，再让目标模型一次验证多个位置，用少量便宜计算减少主模型的串行等待。**

![DSpark 总体原理](assets/dspark/01-overview.png)

---

## DSpark 解决哪两个问题？

DSpark 是一个用于加速大模型推理的投机解码框架。它主要解决传统投机解码中的两个瓶颈：

| 传统方法的问题 | 直接后果 | DSpark 的解决办法 |
|---|---|---|
| 草稿越往后越不准 | 接受率快速下降 | 半自回归生成 |
| 不管质量如何都验证整段草稿 | 浪费目标模型算力 | 置信度调度验证 |

### 问题一：草稿质量衰减

并行草稿模型可以一次快速产生一串候选 Token，但各个位置缺少足够的上下文依赖：

```text
草稿前半段：比较可靠
草稿后半段：越来越容易偏离
```

DSpark 使用**半自回归生成**解决这个问题：

```text
并行主干网络
负责一次生成整个草稿块的“骨架”
            +
轻量顺序头
按顺序注入局部 Token 依赖
```

这样既保留了并行生成的速度，又改善了草稿后半段的连贯性，减缓接受率下降。

### 问题二：验证效率低下

如果把整段长草稿全部交给目标模型，即使后半段被接受的概率已经很低，目标模型仍然要为这些位置计算。

DSpark 使用**置信度调度验证**减少这种浪费：

```text
置信度头
预测各位置被接受的可能性
            ↓
硬件感知调度器
结合置信度和系统负载决定送检长度
```

- 系统负载低：可以验证更长的草稿，争取提高单用户生成速度；
- 系统负载高：裁掉低置信度后缀，减少无效验证，保护整体吞吐量。

可以把 DSpark 的核心概括为：

> **一边提高草稿质量，一边减少无效验证。**

---

## 1. 大模型为什么一个 Token 一个 Token 地输出？

大模型生成内容的基本单位叫 **Token**。为了方便理解，可以暂时把它看成一个字或一个词。

```text
北 → 京 → 是 → 中 → 国 → 的 → 首 → 都
```

后一个 Token 依赖前面的结果，所以普通生成必须一轮接一轮：

```text
第 1 轮 → Token 1
第 2 轮 → Token 2
第 3 轮 → Token 3
第 4 轮 → Token 4
第 5 轮 → Token 5
```

问题不是 GPU 不会并行，而是未来 Token 还不存在，模型不知道后面的上下文。

---

## 2. 投机解码：让草稿模型提前猜

投机解码安排两个角色：

```text
DSpark 草稿模型：提前提出候选
目标模型：验证并决定最终输出
```

可以把它们想成助教和老师：

1. 助教快速写一份草稿；
2. 老师一次检查整段；
3. 前面通过的连续采用；
4. 遇到第一次拒绝就停止。

![老师与助教类比](assets/dspark/02-teacher-analogy.png)

最重要的一点：

> **草稿模型只有建议权，目标模型拥有决定权。**

草稿模型也不是随便找来的通用小模型，而是专门学习目标模型的输出行为。两者越匹配，草稿越容易通过验证。

---

## 3. 主模型也要重新计算，为什么还能更快？

是的，目标模型仍然要计算。

区别在于：

```text
普通生成：主模型运行 5 轮，每轮生成 1 个 Token
投机解码：草稿模型先猜，主模型运行 1 轮验证多个位置
```

![五轮串行与一轮并行检查](assets/dspark/03-serial-vs-parallel.png)

一次检查 5 个位置的计算量并不等于只生成 1 个 Token，但 GPU 更擅长一次处理多个位置，通常比连续启动主模型 5 轮更快。

所以，小模型购买的是：

> **让主模型并行检查多个未来位置的机会。**

---

## 4. 四个概念不要混淆

下面这张图把投机解码、自回归草稿、完全并行草稿和半自回归生成放在了一起：

![投机解码与三种草稿生成方式](assets/dspark/05-drafting-modes.png)

### 投机解码

完整的加速框架：**草稿模型提出候选，目标模型统一验证。**

### 自回归草稿

```text
Token 1 → Token 2 → Token 3 → Token 4
```

优点是连贯，缺点是草稿模型也要逐个等待。

### 完全并行草稿

```text
[Token 1] [Token 2] [Token 3] [Token 4]
               一次产生
```

优点是快，缺点是后面的 Token 缺少前面候选的信息，越往后越容易猜错。

### 半自回归生成

DSpark 的做法是：

```text
主要计算并行完成
       +
少量依赖顺序完成
```

它不是一种新的验证框架，而是 DSpark **生成草稿的方法**。

---

## 5. DSpark 怎样生成草稿？

DSpark 使用“并行主干 + 轻量顺序头”。

![DSpark 半自回归草稿](assets/dspark/06-semi-autoregressive.png)

### 并行主干

一次产生整个候选块的基础预测，避免草稿模型为了猜多个 Token 运行同样数量的串行轮次。

### 轻量顺序头

让后面的候选参考前面的候选，补回 Token 之间的局部联系。

```text
并行主干：负责快
轻量顺序头：负责连贯
```

昂贵的主要计算仍然并行完成，只有轻量修正需要顺序执行。这就是**半自回归生成**。

---

## 6. 目标模型如何验证草稿？

草稿已经存在后，目标模型可以通过一次前向计算，同时得到多个位置的预测结果。

注意：

```text
计算方式：多个位置并行计算
验收规则：从左到右执行
```

![目标模型验证与 bonus Token](assets/dspark/07-verification-bonus.png)

### 场景 A：中途拒绝

```text
T1 ✓  T2 ✓  T3 ✓  T4 ✗  T5 丢弃
```

接受 T1～T3；T4 被拒绝后，后面的草稿全部丢弃，再由目标模型给出修正 Token。

### 场景 B：全部通过

如果整个草稿块都通过，目标模型通常还能利用这次计算产生一个额外的 **bonus Token**。

---

## 7. “验证通过”是比较两个 Token 吗？

在贪心解码中，可以近似理解为：

```text
草稿 Token = 目标模型最想输出的 Token？
```

随机采样时则不是简单比较相等。系统会参考草稿模型概率和目标模型概率，通过接受—拒绝规则决定是否采用。

这样才能保证：

> **加速后的结果仍服从目标模型原来的输出分布。**

本文不展开数学证明，只需要记住：草稿模型负责提议，目标模型的概率分布仍然是最终标准。

---

## 8. 为什么不能一次猜 100 个 Token？

因为草稿越长，后面的候选通常越不可靠：

```text
T1 ✓  T2 ✓  T3 ✓  T4 ✗  T5～T100 全部丢弃
```

主模型虽然检查了很长的草稿，最后却只接受 3 个 Token，后面的计算就浪费了。

所以草稿不是越长越好，真正的问题是：

> **这一轮送给主模型检查多长最划算？**

---

## 9. DSpark 如何决定送检长度？

DSpark 还包含两个重要角色：

```text
置信度头：估计各位置被接受的可能性
硬件感知调度器：决定实际送检长度
```

调度器会综合考虑：

- 每个候选的置信度；
- 连续前缀能够存活到多长；
- 验证不同长度草稿的效率；
- 当前批量大小和服务器负载。

![投机解码的收益与成本](assets/dspark/04-cost-benefit.png)

服务器不忙时，可以验证更长的草稿，争取让单个用户更快。

服务器很忙时，会裁掉低置信度后缀，避免无效 Token 占用其他请求的计算空间。

---

## 10. DSpark 一定会更快吗？

不一定。

```text
减少的大模型串行等待
            >
草稿模型成本 + 无效验证成本
```

满足下面这些条件时更容易加速：

- 草稿模型足够快；
- 草稿与目标模型足够匹配；
- 每轮能连续接受多个 Token；
- 送检长度选择合理。

如果第一个或第二个 Token 经常被拒绝，小模型就可能成为额外负担。

---

## 11. DSpark 优化推理的哪个阶段？

![Prefill 与 Decode](assets/dspark/08-prefill-decode.png)

### Prefill：读取用户输入

输入 Token 已经全部存在，本来就可以并行处理。DSpark 不是主要优化这里。

### Decode：生成答案

新 Token 要逐个产生，串行等待最明显。DSpark 主要优化这里。

```text
主模型训练：DSpark 不参与
草稿模型训练：需要提前完成
Prefill：不是主要优化对象
Decode：DSpark 的主要加速对象
```

DSpark 不会增加目标模型的知识或能力，它只负责让在线生成更快。

---

## 12. 最后记住这张关系图

```text
用户请求
   ↓
目标模型完成 Prefill
   ↓
DSpark 并行主干产生候选块
   ↓
轻量顺序头改善候选连贯性
   ↓
置信度头估计接受概率
   ↓
调度器裁剪送检长度
   ↓
目标模型一次并行计算
   ↓
按前缀规则验收
   ↓
输出通过的 Token
```

一句话总结：

> **DSpark 用并行主干快速起草，用轻量顺序头提高连贯性，再按置信度和服务器负载决定验证多长，最终由目标模型统一验证。**

---

## 延伸阅读

- [详细谈谈 DSpark 投机解码的原理](https://mp.weixin.qq.com/s/RRHg9UCCInSc_zEcIgjNBQ)
- [DSpark 论文](https://arxiv.org/abs/2607.05147)
- [DeepSpec 官方开源仓库](https://github.com/deepseek-ai/DeepSpec)
