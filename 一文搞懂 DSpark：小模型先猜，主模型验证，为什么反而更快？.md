# 一文搞懂 DSpark：小模型先猜，主模型验证，为什么反而更快？

DSpark 是 DeepSeek 联合北京大学提出的一套投机解码方案，用来加快大模型生成答案的速度。[论文](https://arxiv.org/abs/2607.05147)于 2026 年 7 月 6 日提交，其中还记录了 DSpark 接入 DeepSeek-V4 线上服务后的实际运行效果。

它的思路不绕。先让一个速度快、计算量小的草稿模型猜出后面的几个 Token，再交给目标模型一次检查。猜对的直接采用；碰到第一个错误就停下，丢掉后面的草稿，由目标模型补上一个新 Token。前面虽然多了“猜答案”这一步，目标模型需要一轮轮等待的次数却少了，整体生成反而更快。

![DSpark 总体原理](assets/dspark/01-overview.png)

---

## 0. DSpark 要解决什么问题？

传统投机解码有两个麻烦：

| 传统方式带来的问题 | 会带来什么影响 | DSpark 的解决办法 |
|---|---|---|
| 草稿越往后越不准 | 接受率快速下降 | 半自回归生成 |
| 不管质量如何都验证整段草稿 | 浪费目标模型算力 | 置信度调度验证 |

半自回归生成负责把草稿写准一些，置信度调度验证负责少做无用功。

---

## 1. 大模型为什么一个 Token 一个 Token 地输出？

大模型生成内容的基本单位叫 **Token**。这里先简单理解成一个字或一个词就行。

```text
北 → 京 → 是 → 中 → 国 → 的 → 首 → 都
```

后一个 Token 要参考前面的结果，所以普通生成只能一轮接一轮：

```text
第 1 轮 → Token 1
第 2 轮 → Token 2
第 3 轮 → Token 3
第 4 轮 → Token 4
第 5 轮 → Token 5
```

GPU 可以并行计算，问题是未来的 Token 还没出现，模型手里没有后面的上下文。

---

## 2. 什么是投机解码？

投机解码里有两个角色：

```text
DSpark 草稿模型：提前提出候选
目标模型：验证并决定最终输出
```

可以把它们看成助教和老师：

1. 助教快速写一份草稿；
2. 老师一次检查整段；
3. 前面通过的连续采用；
4. 遇到第一次拒绝，就停止验收当前草稿块，丢弃后续草稿；
5. 目标模型产生一个新 Token，与通过的前缀一起提交，然后进入下一轮。

![老师与助教类比](assets/dspark/02-teacher-analogy.png)

草稿模型只负责交草稿，最后输出什么，仍由目标模型决定。

这里的草稿模型也不能随便找一个通用小模型代替。它需要针对目标模型专门训练，两者越合拍，草稿通过得越多。

---

## 3. 主模型也要重新计算，为什么还能更快？

目标模型当然还要算，变化在于怎么计算：

```text
普通生成：主模型运行 5 轮，每轮生成 1 个 Token
投机解码：草稿模型先猜，主模型运行 1 轮验证多个位置
```

![五轮串行与一轮并行检查](assets/dspark/03-serial-vs-parallel.png)

一次检查 5 个位置，不会便宜到和生成 1 个 Token 一样。不过 GPU 擅长成批处理，这通常还是比让目标模型连续跑 5 轮更快。

草稿模型的作用就在这儿。它先把候选摆出来，目标模型才有机会一次检查多个未来位置。

---

## 4. 自回归、完全并行和半自回归有什么区别？

投机解码管的是“先起草，再验证”。至于这份草稿怎么写出来，常见做法有三种：

![投机解码与三种草稿生成方式](assets/dspark/05-drafting-modes.png)

### 自回归草稿

```text
Token 1 → Token 2 → Token 3 → Token 4
```

写出来比较连贯，但草稿模型自己也得逐个等待。

### 完全并行草稿

```text
[Token 1] [Token 2] [Token 3] [Token 4]
               一次产生
```

速度快，不过后面的 Token 看不到前面刚猜出的内容，通常越往后越容易出错。

### 半自回归生成

DSpark 把两种方式组合起来：

```text
主要计算并行完成
       +
少量依赖顺序完成
```

半自回归说的是 DSpark **怎么生成草稿**，不是另一套验证框架。

---

## 5. 核心创新一：半自回归生成

DSpark 的草稿模型由“并行主干”和“轻量顺序头”组成。

![DSpark 半自回归草稿](assets/dspark/06-semi-autoregressive.png)

### 并行主干

并行主干一次给出整个候选块的基础预测，不用为了猜多个 Token 连跑同样多的串行轮次。

### 轻量顺序头

轻量顺序头再按位置逐个修正和采样，让后面的候选能参考前面已经采样出的内容。论文默认采用 Markov Head，主要补充相邻 Token 的转移信息；作者也测试了能记住更长前缀状态的 RNN Head。

费算力的部分仍然并行完成，只有较轻的修正需要按顺序做。这就是**半自回归生成**。

---

## 6. 目标模型如何验证草稿？

拿到草稿后，目标模型只做一次前向计算，就能得到多个位置的预测结果。

```text
计算方式：多个位置并行计算
验收规则：从左到右执行
```

![目标模型验证与 bonus Token](assets/dspark/07-verification-bonus.png)

### 场景 A：中途拒绝

```text
T1 ✓  T2 ✓  T3 ✓  T4 ✗  T5 丢弃
```

T1～T3 可以留下。T4 没通过，T4 和它后面的草稿都不要了。目标模型在这个位置重新采样一个 Token，和 T1～T3 一起提交，再开始下一轮。

这里的“重新采样”不是把 T4 改成某个标准答案。使用随机采样时，官方评测代码会从目标分布相对草稿分布的**残差分布**中选出这个新 Token。

### 场景 B：全部通过

如果整个草稿块都通过，目标模型会从草稿块末尾对应的目标分布中再采样一个 **bonus Token**。如果本轮已经遇到结束 Token，则生成可以直接结束。

### 补充：随机采样时如何验证？

在贪心解码中，可以近似理解为：

```text
草稿 Token = 目标模型最想输出的 Token？
```

到了随机采样，验证就不能只看两边是否相等。草稿 Token \(x_k\) 的标准接受概率是：

```text
min(1, 目标模型给 x_k 的概率 ÷ 草稿模型给 x_k 的概率)
```

第一次拒绝后，后续草稿全部丢弃，并从残差分布采样一个新的目标模型 Token。

这套接受和拒绝规则能让加速后的结果继续服从目标模型原来的输出分布。数学证明可以先放一边，只需记住一点：草稿模型给建议，目标模型的概率分布说了算。

---

## 7. 核心创新二：置信度调度验证

草稿写得越长，后面的候选通常越不可靠：

```text
T1 ✓  T2 ✓  T3 ✓  T4 ✗  T5～T100 全部丢弃
```

目标模型检查了一长段，最后只留下 3 个 Token，后面的计算全白费了。

所以草稿并非越长越好。系统每一轮都要判断，送多长的草稿去检查才划算。

DSpark 用两个组件来做这件事：

```text
置信度头：估计“此前位置都已接受时，本位置继续被接受”的条件概率
硬件感知调度器：决定实际送检长度
```

调度器做决定时会参考这些信息：

- 每个候选的置信度；
- 连续前缀能够存活到多长；
- 验证不同长度草稿的效率；
- 当前批量大小和服务器负载。

![投机解码的收益与成本](assets/dspark/04-cost-benefit.png)

服务器不忙，可以多检查几个候选，让单个用户更快拿到答案。

服务器忙起来，就裁掉把握不大的后半段，免得它们挤占其他请求的计算空间。

这里要把论文和开源代码分开看。论文写了完整的硬件感知前缀调度器，也给出了线上部署结果。公开的 DeepSpec 仓库目前以草稿模型训练和离线评测为主，评测代码可以按置信度阈值截断送检前缀，但完整的生产调度系统没有开源。

---

## 8. 使用 DSpark 能带来哪些收益？

先看论文数据。每组结果对应的模型、负载和对照基线不同，不能把它们混成一个通用的加速倍数：

| 测试场景 | 论文报告的结果 |
|---|---|
| DeepSeek-V4-Flash，相同聚合吞吐能力下 | 单用户生成速度提高 60%～85% |
| DeepSeek-V4-Pro，相同系统容量下 | 单用户生成速度提高 57%～78% |
| 中等 SLA：Flash 80 tok/s/user | 相比 MTP-1，聚合吞吐提高 51% |
| 中等 SLA：Pro 35 tok/s/user | 相比 MTP-1，聚合吞吐提高 52% |
| Qwen3-4B、8B、14B 离线评测 | 相比 Eagle3，宏平均接受长度分别提高 30.9%、26.7%、30.0% |

在严格 SLA 下，论文还给出了 661%（Flash）和 406%（Pro）的名义吞吐优势。这个数字看着很大，作者也专门作了提醒：此时 MTP-1 已经接近运行边界，只能维持很小的并发批次。它更适合用来说明 DSpark 把可用的交互速度边界推远了，不能当成一般场景的典型加速倍数。

### 降低 TPOT

DSpark 加速的重点在 Decode 阶段。如果目标模型一轮能收下多个 Token，每个输出 Token 分摊到的时间就会变少，TPOT（Time Per Output Token）通常也会下降。

```text
TPOT ≈ 一轮草稿与验证的耗时 ÷ 这一轮实际产出的 Token 数
```

一轮留下的 Token 越多，这种效果一般越明显。

论文直接报告的是每用户生成速度（tok/s/user）和系统吞吐，没有单列一个名为 TPOT 的指标。文中所说的“降低 TPOT”，是根据单位 Token 延迟公式和生成速度的提升推出来的，不能理解成任何负载下都会按固定比例下降。

### 提高单用户生成速度

普通解码要让目标模型连续跑很多轮。DSpark 把其中一部分串行轮次换成一次并行验证，每秒输出的 Token 也就更多。

### 高并发下减少验证浪费

长草稿的后半段如果大概率通不过，继续验证只会占着批处理空间。DSpark 根据置信度和系统负载缩短草稿，高并发时能少浪费一些算力。

### 不改变目标模型的输出分布

草稿模型只提候选，最终结果仍由目标模型按接受和拒绝规则决定。只要无损投机解码实现正确，加速不会牺牲模型质量。

把这些变化放到常见指标里，大致是这样：

| 指标 | 可能的改善 |
|---|---|
| TPOT | 降低 |
| 输出速度 | 提高 |
| 无效验证计算 | 减少 |
| 高并发吞吐稳定性 | 提高 |
| TTFT | 通常不是主要改善目标 |

---

## 9. DSpark 一定会更快吗？

不一定。省下的目标模型等待时间，得多过生成草稿和无效验证花掉的时间，系统才会真的变快。

下面几种情况更容易跑出加速效果：

- 草稿模型足够快；
- 草稿与目标模型足够匹配；
- 每轮能连续接受多个 Token；
- 送检长度选择合理。

要是第一个或第二个 Token 经常通不过，草稿模型反倒可能成为额外负担。

---

## 10. DSpark 优化推理的哪个阶段？

![Prefill 与 Decode](assets/dspark/08-prefill-decode.png)

### Prefill：读取用户输入

输入 Token 已经全部摆在这里，本来就能并行处理。DSpark 的重点不在 Prefill。

### Decode：生成答案

生成答案时，新 Token 要逐个出现，串行等待最明显。DSpark 主要加速的就是 Decode。

```text
目标模型预训练和后训练：DSpark 不参与
DSpark 准备阶段：冻结目标模型，单独训练草稿模型及置信度头
Prefill：不是主要优化对象
Decode：DSpark 的主要加速对象
```

训练 DSpark 时，目标模型保持冻结，共享的词嵌入层和语言模型输出头也不更新。真正参与训练的是草稿主干、顺序模块和置信度头。DSpark 不会让目标模型懂得更多，只会让它在线生成得更快。

---

## 11. 把完整流程串起来

![一张图总结 DSpark](assets/dspark/09-dspark-end-to-end-summary.png)

DSpark 没有绕过目标模型。它只是想办法减少串行轮次，让同样的生成工作少等几回。

---

## 延伸阅读

- [详细谈谈 DSpark 投机解码的原理](https://mp.weixin.qq.com/s/RRHg9UCCInSc_zEcIgjNBQ)
- [DSpark 论文](https://arxiv.org/abs/2607.05147)
- [DeepSpec 官方开源仓库](https://github.com/deepseek-ai/DeepSpec)
- [官方评测中的接受—拒绝与新 Token 生成代码](https://github.com/deepseek-ai/DeepSpec/blob/main/deepspec/eval/base_evaluator.py)
- [官方 DSpark 草稿与置信度截断代码](https://github.com/deepseek-ai/DeepSpec/blob/main/deepspec/eval/dspark/draft_ops.py)
