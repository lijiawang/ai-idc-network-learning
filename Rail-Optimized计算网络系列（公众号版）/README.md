# 从零设计 Rail-Optimized 计算网络：从 256 卡到万卡集群

本系列拆为三篇公众号稿，按入门、计算、扩容递进。每篇正文可独立复制到公众号编辑器，标题编号保持连载顺序。

| 顺序 | 稿件 | 读完能解决什么 |
|---|---|---|
| 第一篇 | [什么是 Rail-Optimized，为什么从 256 卡起步](./01-入门篇：什么是Rail-Optimized，为什么从256卡起步.md) | 分清 GPU、NIC、Rail、Leaf 和 1∶1，算出 SU 接入容量 |
| 第二篇 | [从 256 卡到 2048 卡，Leaf 和 Spine 怎么算](./02-设计篇：从256卡到2048卡，Leaf和Spine怎么算.md) | 独立算出两层网络的设备与链路，理解按最终规模预留 |
| 第三篇 | [按 Pod 走向万卡，Core 怎么算与怎么预留](./03-扩容篇：按Pod走向万卡，Core怎么算与怎么预留.md) | 区分两层与三层 Pod，算 Core 分组、当前容量与扩容容量 |

## 统一口径

- 每台服务器八张 GPU、八个 400G 计算端口，交换机提供 64 个可同时工作的 400G 逻辑端口。
- 容量方法可用于符合这些条件的 RoCEv2 或 IB 网络，配置与认证范围分别说明。
- 一个 SU 按 32 台服务器、256 张 GPU 计算；一个建设 Pod 按四个 SU、1024 张 GPU 定义。
- 第二篇的独立两层 Pod 与第三篇向 Core 上联的三层 Pod 是不同设计选择，不能按相同接法连续扩展。
- 十个 Pod 是 10240 卡。160 台 Core 只作为当前端口容量下限；256 台明确属于按十六个 Pod 预留的示例，不写成十个 Pod 的必需配置。
- 设备数量与逻辑拓扑为教学算例，不能替代施工接线、路由验证和性能验收。

## 发布前使用

标题按卡数表述规模，正文在需要计算网口时换算成服务器台数。保留原文中的来源链接和限定条件；系列稿不包含未经验证的 192 台 Core 详细示例，也不声称读取或复述了未能获取正文的飞书文档。

三篇均为 Markdown 文本稿，尚未发布到公众号。移动端排版时可将公式作为独立段落，表格按手机宽度预览，避免直接使用超宽代码块。
