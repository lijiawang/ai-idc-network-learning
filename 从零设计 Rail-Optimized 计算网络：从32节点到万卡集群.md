# 从零设计 Rail-Optimized 计算网络：从 32 节点到万卡集群

设计 AI 计算网络，最容易陷入的误区是先记设备数量：128 台服务器配多少 Leaf，512 台配多少 Core，万卡再翻几倍。

真正需要掌握的是一套计算顺序：**先数服务器计算网口，再分配交换机端口，最后根据最终规模决定接线与预留。** 设备数量算够之后，还要检查连接关系是否成立。

本文以 H100、400G InfiniBand 和 64 口交换机为例，从一台服务器开始，推导 SU、Pod、Spine 和 Core 的设计。目标是计算网络正常状态下逐层 1:1，并讨论如何分期建设、减少旧链路改接。

## 1. 先确定条件，不急着选交换机数量

统一采用以下假设，后续数字都在这些条件下成立：

| 项目 | 本文假设 |
|---|---|
| 单台服务器 | 8 张 H100 GPU |
| 计算网络接口 | 每台提供 8 个 400G IB 端口 |
| 交换机 | 每台提供 64 个 400G 逻辑端口 |
| Leaf 端口分配 | 32 个下联、32 个上联 |
| 网络目标 | 正常状态下逐层 1:1，不超售计算带宽 |
| SU | 32 台服务器，256 张 GPU |
| 建设 Pod | 本文定义为 4 个 SU，即 128 台服务器、1024 张 GPU |

**GPU 数量和网口数量是两项独立的配置。** 一台服务器有 8 张 GPU，并不意味着它必然有 8 个 400G 计算端口。整机选型时，必须核实网卡配置、PCIe 带宽以及 GPU—NIC 亲和关系。

本文的 Pod 是项目交付模块，不是 Kubernetes Pod，也不是各厂商统一规定的固定容量单位。

所有设备与链路计算先只统计计算业务端口，不包含 UFM 等辅助节点接入、备件和故障后满带宽冗余。实际采购时需要另行安排，不能在满配端口表上直接追加。

## 2. 一台服务器怎样接入计算网络

同一台 HGX H100 服务器里的 GPU，可以通过机内 NVLink／NVSwitch 通信。跨服务器时，则需要通过网卡和外部交换机。

先把服务器想成一个有八个高速出入口的设备，每个出入口都是 400G：

```text
单台服务器计算端口总带宽
= 8 × 400Gb/s
= 3.2Tb/s
= 400GB/s
```

这里是单向端口线速总和，不代表实际训练一定达到这个吞吐。后面的带宽计算也统一采用单向口径，避免把双向交换容量与单向链路带宽混在一起。

直接连接服务器的第一层交换机，叫作 Leaf。

## 3. 什么叫 1:1 无收敛

可以把 Leaf 想成高速公路汇入口：下面有 32 条 400G 车道，上面也有 32 条 400G 车道，汇入之后没有变窄。

| Leaf 的端口方向 | 端口数 | 单向总带宽 |
|---|---:|---:|
| 下联，接服务器 | 32 × 400G | 12.8Tb/s |
| 上联，接上层交换机 | 32 × 400G | 12.8Tb/s |

下联与上联的带宽相等，就是这里所说的 1:1。

如果下联 32 个 400G，上联只有 16 个 400G，就变成了 2:1。负载低时不一定有区别，但所有下联同时向上发送时，上联最多承载其总线速的一半。

有三个边界要记住：

- **看的是总带宽，不是必须插满端口。** 下联 16 个 400G、上联 16 个 400G，其余留空，也满足 1:1。
- **上下联只是拓扑位置。** 链路本身可以双向传输，并不是上联只能发、下联只能收。
- **1:1 是容量配比，不是任何负载下都不拥堵。** 接线结构、路由分配、目的端口竞争和硬件故障，仍然会影响吞吐。不能只凭上下行相等就宣称全网严格无阻塞。

## 4. Rail-Optimized：先把服务器网口接得有规律

把 32 台服务器分成一组，配置 8 台 Leaf。服务器计算端口统一编号为 NIC0～NIC7：

| 服务器端口 | 连接对象 |
|---|---|
| 所有 32 台服务器的 NIC0 | 本组 Leaf 0 |
| 所有 32 台服务器的 NIC1 | 本组 Leaf 1 |
| …… | …… |
| 所有 32 台服务器的 NIC7 | 本组 Leaf 7 |

一台服务器向八台 Leaf 各接一条线。32 台服务器都这样接，每台 Leaf 正好收到 32 条服务器链路。

两边验算：

```text
服务器侧：32 台 × 8 个端口 = 256 个端口
Leaf 侧：8 台 × 32 个下联 = 256 个端口
```

这就是本文一个 SU 的服务器接入部分。NVIDIA H100 参考架构也采用每组 32 节点 Rail 对齐的方式，同 SU 内同 Rail 的通信可以只经过一台 Leaf。[1]

**“同编号接同一台 Leaf”限定在这个 SU 内。** 全集群扩展后，NIC0 会分布在多个 SU 的不同 Leaf 0 上，不可能无限接入一台交换机。

另一个容易混淆的地方是：**Rail-Optimized 不必然等于八张完全隔离的网络。** 可以下层按 Rail 对齐，上层通过 Spine／Core 互通；也可以设计独立 Rail Fabric，但那是另一套可达性与软件约束，不能把两者的容量上限、接线和路由混用。

本文沿用上层互通的设计。GPU 与网卡的映射需要按实际 PCIe／NUMA 拓扑确认，也不能把“NCCL channel 0 永远绑定 Rail 0”当成通用规则。

## 5. 32 台服务器，Spine 怎么算

不同 Leaf 之间通过 Spine 通信。例如，服务器 A 的 NIC0 访问服务器 B 的 NIC1，可以经过 Leaf 0、某台 Spine、Leaf 1。

现在有 8 台 Leaf，每台预留 32 条上联：

```text
Leaf 上联总数 = 8 × 32 = 256 条 400G
```

如果这是一套独立的两层网络，Spine 不再向上接 Core，它的 64 个端口都可以连接 Leaf：

```text
Spine 数量 = 256 ÷ 64 = 4 台
每对 Leaf—Spine 链路 = 32 ÷ 4 = 8 条 400G
```

再从 Spine 侧检查：8 台 Leaf，每台接 8 条，正好占 64 个端口。

因此，32 台服务器的一个紧凑配置是：

**32 台服务器＋8 台 Leaf＋4 台 Spine。**

这里的多条 400G 链路共同提供容量，并非默认一主一备；也不代表单个流就能自动使用所有链路带宽。

## 6. 两层网络如何增加到 128、256 台

沿用 64 口交换机、Leaf 32 下联／32 上联，按不同规模分别构建紧凑配置，可以得到：

| 服务器 | GPU | Leaf | Spine | 每对 Leaf—Spine 链路 |
|---|---:|---:|---:|---:|
| 32 台 | 256 | 8 | 4 | 8 × 400G |
| 64 台 | 512 | 16 | 8 | 4 × 400G |
| 128 台 | 1024 | 32 | 16 | 2 × 400G |
| 256 台 | 2048 | 64 | 32 | 1 × 400G |

计算顺序不变：

```text
计算网口总数 = 服务器数量 × 8
Leaf 数量 = 计算网口总数 ÷ 32
Spine 数量 = Leaf 数量 × 32 ÷ 64
每对 Leaf—Spine 链路 = 32 ÷ Spine 数量
```

表中的每台 Leaf 都连接全网所有 Spine。NVIDIA 的 H100 公开规模表也列出了 128 台对应 32 Leaf／16 Spine、256 台对应 64 Leaf／32 Spine 的配置。[2]

### 每对交换机的链路减少，带宽会不会下降？

会减少的是这一对设备之间的带宽，Leaf 的总上联带宽没有减少：

```text
128 台方案：16 台 Spine × 每台 2 条 × 400G = 12.8Tb/s
256 台方案：32 台 Spine × 每台 1 条 × 400G = 12.8Tb/s
```

它把带宽分散到了更多 Spine 上。实际利用率还取决于路由是否能合理分配流量。

### 为什么不能直接把这张表当成扩容步骤？

因为表里每一行都在按当前规模充分利用端口。上一行改成下一行时，每对交换机的链路数发生了变化，会涉及旧上联重新分配。

**这是一张容量配置表，不是一张免改线的施工计划。**

另外，256 台这个边界只针对本文这种“所有 Leaf 全连接共享 Spine”的两层方案。不同的 Rail 隔离方式、交换机端口容量或拓扑，可能有不同上限。

## 7. 不想改旧线，就按最终规模布置骨架

假设一期 32 台，最终 128 台：从第一期就部署最终需要的 16 台 Spine，每台 Leaf 到每台 Spine 接两条 400G。

服务器与 Leaf 按 SU 分批增加：

| 阶段 | 服务器累计 | Leaf 累计 | Spine | 每台 Spine 已用／空闲端口 |
|---|---:|---:|---:|---|
| 一期 | 32 台 | 8 台 | 16 台 | 16／48 |
| 二期 | 64 台 | 16 台 | 16 台 | 32／32 |
| 三期 | 96 台 | 24 台 | 16 台 | 48／16 |
| 四期 | 128 台 | 32 台 | 16 台 | 64／0 |

新增 SU 时，新 Leaf 接入 Spine 的预留端口。旧服务器到 Leaf、旧 Leaf 到 Spine 的接线都不用重新分配。

代价是一期提前投入更多 Spine。预留不仅包括机柜空间、光纤和配线架，还必须包括交换机端口与带宽。

NVIDIA 也建议未填满 SU 时，按完整 SU 规划网络及连接，暂时留空尚未部署的节点位置。[1] 不改旧线不等于上线操作完全没有影响，扩容仍需验证拓扑发现、路由变化与在运业务。

## 8. 按 128 台一个 Pod 交付，需要三层设计

如果后面要不断新增 Pod，并且跨 Pod 也保持 1:1，就要把公共 Core 纳入设计。

原来独立运行的 128 台方案只有 16 台 Spine，每台 Spine 的 64 个端口全部接满 Leaf，没有上联 Core 的位置。

改成三层后，每台 Spine 也采用 32 下联／32 上联：

| 每个 Pod 的设备 | 数量 | 端口分配 |
|---|---:|---|
| 服务器 | 128 台 | 每台 8 个计算端口 |
| Leaf | 32 台 | 32 口接服务器、32 口接 Spine |
| Spine | 32 台 | 32 口接 Leaf、32 口接 Core |

每台 Leaf 连接本 Pod 的全部 32 台 Spine，每对一条 400G。Spine 数量变成 32 台，是因为每台用于连接 Leaf 的端口从 64 个减少到了 32 个。

一个 Pod 的带宽检查如下：

| 位置 | 单向总带宽 |
|---|---:|
| 服务器计算端口总和 | 128 × 8 × 400G = 409.6Tb/s |
| Leaf → Spine | 32 × 32 × 400G = 409.6Tb/s |
| Spine → Core | 32 × 32 × 400G = 409.6Tb/s |

这就固定了一个可以向外提供完整带宽的建设模块：**128 台服务器＋32 Leaf＋32 Spine。**

## 9. 怎样通过 Spine 数量计算 Core 数量

端口速率相同的情况下：

```text
Core 端口容量下限
= 向上取整（Spine 总数量 × 每台 Spine 上联端口数 ÷ 每台 Core 可用端口数）
```

四个 Pod 一共有 128 台 Spine，每台上联 32 口：

```text
Core = 128 × 32 ÷ 64 = 64 台
```

注意两点：

1. 用的是全网 Spine 总数量，不是单个 Pod 的 Spine 数量。
2. 这首先是容量下限，必须再验证接线、可达性和路径容量。端口总数相等本身不能证明无阻塞。

在本文固定条件下，如果端口能够充分利用，Core 数量恰好是 Spine 总数的一半。这不是适用于所有设备的通用比例。

## 10. 四个 Pod，64 台 Core 如何接线

采用一种具体的对称分组方案：64 台 Core 分成 32 组，每组两台。每个 Pod 的 Spine 都编号为 S1～S32。

| Core 组 | 连接对象 | 每对 Spine—Core 链路 |
|---|---|---:|
| 第 1 组，两台 Core | 四个 Pod 各自的 S1 | 16 条 400G |
| 第 2 组，两台 Core | 四个 Pod 各自的 S2 | 16 条 400G |
| …… | …… | …… |
| 第 32 组，两台 Core | 四个 Pod 各自的 S32 | 16 条 400G |

从两端验算：

```text
每台 Spine 上联：2 台 Core × 16 条 = 32 个端口
每台 Core 接入：4 个 Pod × 16 条 = 64 个端口
```

### 全网有 64 台 Core，为什么一台 Spine 只连两台？

因为 Core 按 Spine 编号分组。每台 Leaf 连接本 Pod 的全部 32 台 Spine，可以通过它们到达全部 32 个 Core 组。

例如，Pod 1 的服务器访问 Pod 2 的服务器，可以经过：

**源服务器 → 源 Leaf → Pod 1 的 S5 → 第 5 组某台 Core → Pod 2 的 S5 → 目的 Leaf → 目的服务器。**

目的 Pod 的 S5 连接了本 Pod 全部 Leaf，因此能够到达目的服务器。也可以选择其他 Spine 编号对应的路径。

这是一套明确的接线示例。其总设备数为 512 台服务器、128 Leaf、128 Spine、64 Core，与 NVIDIA 的 512 台规模表一致；数量一致不意味着本文分组就是官方唯一接法。[2]

## 11. 从四个 Pod 扩展到十六个，为什么接法会变

若最终要支持十六个 Pod，可以让每台 Core 给每个 Pod 分配四个端口：

```text
64 个 Core 端口 ÷ 16 个 Pod = 每个 Pod 4 个端口
32 个 Spine 上联 ÷ 每台 Core 4 条 = 每台 Spine 连接 8 台 Core
```

于是，每个 Core 组从两台变成八台，仍然按 Spine 编号划分 32 组：

```text
Core 总数 = 32 组 × 8 台 = 256 台
```

| 规划容量 | 每组 Core | 每对 Spine—Core 链路 | 每台 Spine 上联 | Core 总数 |
|---|---:|---:|---:|---:|
| 4 个 Pod | 2 台 | 16 条 | 32 条 | 64 台 |
| 8 个 Pod | 4 台 | 8 条 | 32 条 | 128 台 |
| 16 个 Pod | 8 台 | 4 条 | 32 条 | 256 台 |

这些对称方案让每台 Core 为更多 Pod 分配更少端口，同时让每台 Spine 连接更多 Core，总上联带宽不变。

**四 Pod 的“到两台 Core 各接 16 条”，不能直接当作十六 Pod 的免改线扩容起点。** 最终规模不同，初始接线就应不同。

## 12. 十个 Pod 就是万卡，为什么不一定要配 256 台 Core

每个 Pod 1024 张 GPU，因此：

```text
10 个 Pod = 1280 台服务器 = 10240 张 GPU
Leaf = 10 × 32 = 320 台
Spine = 10 × 32 = 320 台
```

只按当前端口总量计算：

```text
Core 容量下限 = 320 × 32 ÷ 64 = 160 台
```

**256 台 Core 是“按十六个 Pod 预留”的一种选择，不是十个 Pod 的必需数量。** 是否要花这笔预留成本，应由扩容计划决定。

但 160 台也不能不经设计就拿来下单。若沿用 32 个固定 Core 组，每组只有五台，32 条 Spine 上联不能平均分给五台 Core。需要研究不同连接分配及其容量，不能仅用四舍五入补齐。

### 只做十个 Pod，还有更省设备的规整方案吗？

有。下面给出一套独立推导的容量与连接示例，用于说明“不必直接预留到十六个 Pod”，不作为未经验证的采购定案。

保留 32 个 Core 组，**每组配置六台，共 192 台 Core**。每台 Spine 到对应组六台 Core 的链路数分配为：

```text
6 + 6 + 5 + 5 + 5 + 5 = 32 条 400G
```

十个 Pod 使用相同分配。检查一个 Core 组：

| 组内 Core | 每个 Pod 接入链路 | 十个 Pod 合计占用 | 剩余端口 |
|---|---:|---:|---:|
| Core A、B，各一台 | 6 条 | 每台 60 口 | 每台 4 口 |
| Core C、D、E、F，各一台 | 5 条 | 每台 50 口 | 每台 14 口 |

这样每台 Spine 保持 32 条上联，每台 Core 都不超过 64 口，各 Pod 的连接容量也对称。不同 Core 承担的链路数量不相等，路由应按实际路径容量分配，不能把六台 Core 简单当成六个等容量出口。

这个方案的 Core 总数：

```text
32 组 × 6 台 = 192 台
```

它没有承诺 160 台的理论下限可达，也没有宣称 192 台是全局最优。它说明：**允许单对交换机的链路数不完全相同，可以在保持每台 Spine 上联总带宽的同时，减少端口浪费。** 真正的无收敛验收还要检查端到端路径容量与路由，而不只是这些加法。

该接法无法原样加入第十一个完整 Pod：Core A、B 各会需要 66 个端口。虽然其他 Core 还有空口，也不能当成可直接使用的完整 Pod 容量。

### 十个 Pod 的三种数量口径

| 口径 | Core 数量 | 应怎样理解 |
|---|---:|---|
| 当前端口容量下限 | 160 台 | 只有总量结论，尚未给出满足目标的完整拓扑 |
| 按十个 Pod 的六台一组示例 | 192 台 | 上联按 6、6、5、5、5、5 分配，需验证路由与性能 |
| 按十六个 Pod 的八台一组预留 | 256 台 | 每对四条，保留新增六个 Pod 的连接容量 |

选择顺序应当是：**先确认最终容量、扩容约束与可接受的预留成本，再决定 Core 组的规模。**

## 13. 什么时候才算“万卡交付完成”

交换机数量和链路数量算通，只完成了逻辑设计。交付至少要补齐以下内容：

| 交付项 | 需要明确的结果 |
|---|---|
| GPU—NIC 映射 | GPU、PCIe／NUMA、HCA、物理端口和 Rail 的对应关系 |
| 两端端口表 | 每条链路的源设备／端口、目的设备／端口、速率及预留用途 |
| 光模块与布线 BOM | 物理接口、兼容性、模块型号、线长、配线架、标签和备件 |
| 管理节点接入 | UFM／Subnet Manager 等所需端口、主备方式和可达性 |
| 路由与拥塞控制 | 根据真实拓扑选择 IB 路由、路径分配与拥塞管理方案 |
| 通信软件验证 | NCCL 实际选择的 HCA、GPU Direct RDMA 状态与链路利用率 |
| 分级性能验收 | 单链路、单 SU、单 Pod、跨 Pod 的 RDMA 与集合通信测试 |
| 故障验收 | 断链路、断 Spine、断 Core 后的剩余容量、告警和作业恢复 |

应覆盖 AllReduce，也应根据业务测试 All-to-All 等不同通信模式，不能只凭一次 ping 通或者一个带宽数字签收整网。

NCCL 的网卡选择和 `NCCL_CROSS_NIC` 等设置需要结合拓扑与版本验证，不能把环境变量当成物理网络设计的替代品。[4]

如果使用 RoCE，才需要按对应方案规划以太网路由以及 PFC／ECN 等机制。本文计算案例是 IB，不能把 eBGP、PFC 配置直接套到 IB 交换机上。

### 逻辑链路不等于成品线缆

QM9700／QM9790 提供 64 个 400G NDR 逻辑端口，但面板是 32 个 OSFP 插槽，每个插槽承载两个 400G 端口。[3]

因此，文中的“16 条 400G”说的是逻辑链路。它对应多少光模块、多少根成品线缆，要由双端口模块、服务器接口、线缆组合和距离共同决定。

### 正常时 1:1，不等于故障后仍然 1:1

如果一台 Leaf 恰好是 32 条下联、32 条上联，断掉上联后可用容量就下降了。多路径提供了替代路线，但不会凭空补回损失的带宽，也不保证运行中的训练任务无中断。

如果要求故障后仍然保持原有满带宽，就必须把额外容量单独设计进去。正常容量、故障容量和未来扩容容量，是三笔不同的账。

## 14. 留在手边的设计公式

以下变量都针对选定的计算网络，端口带宽单位保持一致：

```text
N = 服务器数量
R = 每台服务器的计算端口数
B = 单个计算端口带宽
D = 每台 Leaf 的下联端口数
U = 每台 Leaf 的上联端口数
S_down = 每台 Spine 可用于连接 Leaf 的端口数
S_up = 每台 Spine 可用于连接 Core 的端口数
C = 每台 Core 的可用接入端口数
```

```text
服务器计算端口总数 = N × R
服务器单向总带宽 = N × R × B

按 Rail 分组时：
Leaf 数量 = R × ceil(N ÷ D)
（有 SU／Pod 边界时，应按各模块分别取整再求和）

Spine 端口容量下限 = ceil(Leaf 数量 × U ÷ S_down)
Core 端口容量下限 = ceil(Spine 总数 × S_up ÷ C)
```

端口速率不同时，收敛比要改成带宽计算：

```text
下联总带宽 : 上联总带宽
= 下联端口数 × 下联速率 : 上联端口数 × 上联速率
```

这些公式回答“至少需要多少容量”。之后还必须回答“链路接到哪里、各方向是否可达、流量能否用足容量”。

开始一个真实项目时，先写下四句话：

1. 每台服务器提供几个、多快的计算端口。
2. 每个 SU／Pod 放多少台服务器。
3. 当前交付多少台，最终计划多少台。
4. 哪个范围保证 1:1，扩容和故障时允许哪些变化。

这四个条件明确后，再去算 Leaf、Spine、Core 和线缆，设计才会有稳定的依据。

## 参考资料与口径

1. [NVIDIA DGX H100 SuperPOD：Network Fabrics](https://docs.nvidia.com/dgx-superpod/reference-architecture-scalable-infrastructure-h100/latest/network-fabrics.html)。用于核对 SU、Rail 对齐和节点预留。当前完整参考配置包含 UFM 接入调整，因此其部分表格是 31／127 台计算节点；本文 32／128 台是纯计算端口算例。
2. [NVIDIA DGX H100 SuperPOD：Architecture](https://docs.nvidia.com/dgx-superpod/reference-architecture-scalable-infrastructure-h100/latest/dgx-superpod-architecture.html)。用于核对 128、256、512 台等规模的交换机数量。本文详细分组及十 Pod 优化为推导示例，不冒充官方施工接线。
3. [NVIDIA QM97XX：Cable Installation](https://networking-docs.nvidia.com/qm97x0hw/cable-installation)。用于区分 400G 逻辑端口与 OSFP 物理插槽。
4. [NCCL 2.30.3：Environment Variables](https://docs.nvidia.com/deeplearning/nccl/archives/nccl_2303/user-guide/docs/env.html)。用于理解多 NIC、Rail 和通信库选路的关系。

本文整理自拓扑设计讨论，并参考以上官方资料。用户提供的飞书文档正文未能成功读取，因此本文不作为该文档的逐段摘要或已核验复述。
