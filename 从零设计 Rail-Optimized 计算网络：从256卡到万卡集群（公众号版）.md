# 从零设计 Rail-Optimized 计算网络：从 256 卡到万卡集群

一台八卡 GPU 服务器，配了八个计算网口。这八根线，应该接到同一台交换机，还是分别接到八台？

看懂这个接法，再去算千卡、万卡集群需要多少交换机，就容易多了。

这篇从 Rail-Optimized 讲起，沿着服务器接入、交换机分层、分期扩容这条线，把端口数量一步步算出来。例子采用 H100、400G 网络，以及能提供 64 个 400G 逻辑端口的交换机。

网络可以选 **RoCEv2 或 InfiniBand（IB）**。端口条件相同，容量计算方法就能共用；具体设备、路由和拥塞控制配置要分别设计。

## 01 Rail-Optimized，先让网口接得有规律

Rail 可以译作轨道。Rail-Optimized，也就是轨道优化，指的是把不同服务器对应位置的计算网口，按一致的规则接入网络。

先认识两个名字。NIC 指网卡或网络接口，Leaf 是直接连接服务器的第一层交换机。

假设一组服务器都配置八个计算网口，编号为 NIC0～NIC7。我们准备八台 Leaf，按下面的规则接线。

| 服务器上的网口 | 接到哪里 |
|---|---|
| A、B、C……的 NIC0 | Leaf 0 |
| A、B、C……的 NIC1 | Leaf 1 |
| …… | …… |
| A、B、C……的 NIC7 | Leaf 7 |

**一台服务器往八台 Leaf 各接一条线，其他服务器照着接。**

这样，服务器 A 的 NIC0 和服务器 B 的 NIC0 都在 Leaf 0 上。它们之间传数据，只经过这一台交换机。

如果两个网口接在不同 Leaf，就需要经过上层交换机转发。负责连接不同 Leaf 的这一层，叫作 Spine。

Rail 对齐提供了更短的本地同轨路径。通信库结合机内 GPU 互联和网卡位置安排数据交换，就有机会减少经过上层交换机的流量。

这里有两个范围要说清楚。

同编号网口接同一台 Leaf，是在一组服务器内部成立。集群扩大后，同一个 Rail 可以分布在多台 Leaf 上，同轨跨组通信仍然需要上层网络。

另外，Rail-Optimized 可以采用上层互通的网络，并不必然是八张物理隔离的网。本文采用的就是下层 Rail 对齐、上层 Spine 和 Core 互通的方式。NVIDIA H100 参考架构也采用每组 32 节点 Rail 对齐的接入方式。[NVIDIA Network Fabrics](https://docs.nvidia.com/dgx-superpod/reference-architecture-scalable-infrastructure-h100/latest/network-fabrics.html)

实际接线还要核对 GPU 与 NIC 的硬件位置。网口编号必须统一映射，不能把操作系统枚举出来的网卡名字直接当成机箱位置。

## 02 为什么从 256 卡开始

256 卡这个数字，来自交换机端口和服务器配置。

一台交换机有 64 个 400G 端口。如果全部拿来接服务器，后面就没有端口接上层网络了。

我们把 Leaf 分成两半。

**32 个端口接服务器，另外 32 个端口向上连接。**

每增加一台服务器，它的八个计算网口分别接入八台 Leaf，每台 Leaf 就多用一个下联端口。

接到 32 台服务器，八台 Leaf 的下联恰好用满。每台服务器八张 GPU，便得到 32 × 8＝256 张 GPU。

我们把这组 32 台服务器及其接入网络称为一个 SU，可以理解为基本扩展单元。

**256 卡是这套条件下的一个完整模块。** 也可以先上八台服务器、64 张 GPU，留空其余位置。换了交换机端口数、网口速率或服务器配置，模块大小也可能改变。

还有一点，八张 GPU 和八个计算网口是两项独立配置。采购服务器时，要分别确认，不能只凭 GPU 数量推算网口。

## 03 1∶1 无收敛，就是出口不变窄

每台 Leaf 下联 32 个 400G，上联也是 32 个 400G。

下联带宽是 32 × 400G＝12.8Tb/s，上联也是 12.8Tb/s。下面能进来多少，上面就能送出去多少，这就是这里的 1∶1。

如果上联减成 16 个 400G，就只有 6.4Tb/s。低负载时不一定能感受到差别；大家同时往上发，出口就要排队。这叫 2∶1 收敛。

注意，1∶1 看的是总带宽，不要求所有空口都插满。下联 16 个 400G、上联 16 个 400G，也满足这个配比。

**1∶1 也不是不拥堵的保证书。** 多个发送端挤向同一个目的端口，或者路由把流量集中在少数路径上，仍然可能拥堵。端口配比解决的是容量问题，接法和路径分配还要另外检查。

## 04 有了八台 Leaf，Spine 该配几台

先完成一个独立的 256 卡集群。

现在有八台 Leaf，每台有 32 条上联，合计 8 × 32＝256 条 400G 链路。

这是一套两层网络，Spine 不需要再向上连接 Core，所以它的 64 个端口都可以接 Leaf。

Spine 数量就是 256 ÷ 64＝4 台。

每台 Leaf 的 32 条上联，平均分给四台 Spine，每对接八条。反过来检查，一台 Spine 接八台 Leaf，每台八条，正好占用 64 个端口。

得到的配置是 **32 台服务器、8 台 Leaf、4 台 Spine**。

沿用相同方法，可以算出不同规模的紧凑配置。

| GPU 规模 | 服务器 | Leaf／Spine | 每对 Leaf—Spine 链路 |
|---|---|---|---|
| 256 卡 | 32 台 | 8／4 台 | 8 条 400G |
| 512 卡 | 64 台 | 16／8 台 | 4 条 400G |
| 1024 卡 | 128 台 | 32／16 台 | 2 条 400G |
| 2048 卡 | 256 台 | 64／32 台 | 1 条 400G |

NVIDIA 的 H100 规模表也列出了其中 128 台和 256 台服务器对应的交换机数量。[NVIDIA SuperPOD Architecture](https://docs.nvidia.com/dgx-superpod/reference-architecture-scalable-infrastructure-h100/latest/dgx-superpod-architecture.html)

每对交换机的链路减少了，Leaf 的总上行带宽却没减少。到 16 台 Spine 各接两条，与到 32 台 Spine 各接一条，总共都是 32 条 400G。

不过，这张表有个容易忽略的问题。

**每一行是按当前规模算出的配置，不能直接当作免改线的扩容步骤。** 从四台 Spine、每对八条，改成十六台 Spine、每对两条，旧 Leaf 的上联就需要重新分配。

## 05 扩容不想拆旧线，就要提前留出位置

假设一期 256 卡，最终扩到 1024 卡。

可以从第一天就装好最终需要的 16 台 Spine。每台 Leaf 到每台 Spine 固定接两条 400G，服务器和 Leaf 按 SU 分批增加。

一期只有八台 Leaf，每台 Spine 使用 8 × 2＝16 个端口，还剩 48 个。

后面每增加一个 SU，就增加 32 台服务器、八台 Leaf。新 Leaf 接到原有 Spine 的预留端口，旧线不用重新分配。

做到第四个 SU，每台 Spine 的 64 个端口正好用满。

这个选择需要一期提前投入更多 Spine。节省初期预算和减少后续改线，各有成本，应该在采购之前选定。

配线架和预留光纤能让施工方便一些，但它们增加不了交换机的端口容量。真正有用的预留，需要把端口、带宽和机房条件一起算进去。

## 06 按 Pod 增加，需要给 Spine 再留一半端口

为了按模块交付，我们在这里约定一个建设 Pod 包含四个 SU，也就是 128 台服务器、1024 张 GPU。

Pod 大小是项目定义，不能看到这个名字就认定一定有 128 台服务器。这里也不是 Kubernetes Pod。

如果后续的多个 Pod 要一起跑训练，并且跨 Pod 也保持 1∶1，我们可以规划三层网络，通过公共 Core 连接各 Pod。

可前面 1024 卡独立方案的 Spine 已经满了。每台 Spine 连接 32 台 Leaf，每对两条，一共 64 个端口，没有地方接 Core。

解决办法是重新划分 Spine 的端口。

**32 个端口接本 Pod 的 Leaf，32 个端口向上接 Core。**

32 台 Leaf 一共需要 32 × 32＝1024 个 Spine 下联端口。现在每台 Spine 只提供 32 个，所以需要 1024 ÷ 32＝32 台 Spine。

一个可以继续向外连接的 Pod，就固定成了 **128 台服务器、32 台 Leaf、32 台 Spine**。

每台 Leaf 连接本 Pod 全部 32 台 Spine，每对一条 400G。每台 Spine 的另外 32 个端口，留给 Core。

从服务器到 Leaf、从 Leaf 到 Spine、从 Spine 到 Core，一个完整 Pod 的单向总带宽都是 409.6Tb/s。

## 07 Core 数量算出来，还要知道接到哪里

先按四个 Pod、4096 张 GPU 举例。

全网有 4 × 32＝128 台 Spine。每台 Spine 有 32 条上联，每台 Core 能接 64 条，所以容量计算是

**Core＝128 × 32 ÷ 64＝64 台。**

接下来给出一种分组接法。这里是逻辑设计示例，落地前仍需验证路由和端到端容量。

把每个 Pod 的 Spine 编号为 S1～S32，再把 64 台 Core 分成 32 组，每组两台。

四个 Pod 的 S1，都连接 Core 第 1 组；四个 Pod 的 S2，都连接第 2 组。其余按编号对应。

每台 Spine 到组内两台 Core，各接 16 条 400G。这样，Spine 用掉 2 × 16＝32 个上联端口，Core 则用了 4 个 Pod × 16＝64 个端口。

一台 Spine 虽然只连接两台 Core，但它下面的 Leaf 连接了本 Pod 全部 32 台 Spine，可以通过它们到达所有 Core 组。

例如跨 Pod 通信可以走源 Leaf、源 Pod 的 S5、第 5 组的一台 Core、目的 Pod 的 S5，再到目的 Leaf。目的 Pod 的 S5 连接着本 Pod 全部 Leaf，所以能够继续转发到目标服务器。

这里已经能看出，**Core 总数量和单台 Spine 连接多少台 Core，是两个不同的数字。**

## 08 到了万卡，先问清要不要继续扩

一个 Pod 有 1024 张 GPU，十个 Pod 就是 10240 张 GPU，对应 1280 台服务器、320 台 Leaf 和 320 台 Spine。

按当前端口总量算，Core 容量下限是

**320 × 32 ÷ 64＝160 台。**

这个数字先放在纸上，别急着拿它下单。

容量下限只说明需要多少插口，还没有证明链路能按要求接好。沿用前面 32 个 Core 组的结构，每组只有五台，Spine 的 32 条上联无法平均分给它们。

可以研究不等数量的链路分配，也可以增加 Core、留下部分空口。我不会仅凭这道除法，把 160 台写成已验证的万卡采购方案。

另一个常见选择，是按最终十六个 Pod 预留。

这时一台 Core 给每个 Pod 分四个端口，十六个 Pod 正好占 64 个。每台 Spine 有 32 条上联，就要连接八台 Core，每对四条。

仍然按 Spine 编号分 32 组，每组八台，共 **256 台 Core**。

对比两个规划上限，区别就清楚了。

| 规划上限 | 每组 Core | 每对 Spine—Core 链路 | Core 总数 |
|---|---|---|---|
| 4 个 Pod，4096 卡 | 2 台 | 16 条 | 64 台 |
| 16 个 Pod，16384 卡 | 8 台 | 4 条 | 256 台 |

如果按后一种接法建设，交付十个 Pod 时，每台 Core 用掉 40 个端口，留下 24 个，供未来六个 Pod 使用。

**256 台 Core 包含了未来扩容的投入，不能说十个 Pod 必须配这么多。**

如果确定只建设十个 Pod，就应该围绕十个 Pod 专门优化。更细的端口分配适合放进方案评审，逐项检查路径容量、路由能力和故障影响，避免把推导示例写成行业标准。

要做到后续不改旧上联，也得从一期就按最终规则接。按四个 Pod 接好的每对 16 条链路，不能在后面无限往上加 Core。

## 09 线接完了，离交付还有一段路

上面的计算都采用相同速率的逻辑端口，只统计计算业务容量。管理节点接入、备件和故障后满带宽所需的额外容量，要另行规划。

真实设备上，逻辑端口和物理插槽也不一定一一对应。例如 IB 的 QM9700／QM9790 提供 64 个 400G NDR 端口，面板却是 32 个 OSFP 插槽，每个插槽承载两个 400G 端口。RoCEv2 设备要按自己的型号核对，不能照搬这个规格。[NVIDIA QM97XX 线缆说明](https://networking-docs.nvidia.com/qm97x0hw/cable-installation)

RoCEv2 和 IB 还需要不同的配置工作。

RoCEv2 要检查以太网路径、优先级、缓冲区和端侧拥塞控制。典型无损方案会联合使用 ECN 与 PFC，具体取值和机制取决于设备及软件方案。[NVIDIA RoCE 配置说明](https://docs.nvidia.com/networking-ethernet-software/cumulus-linux/Layer-1-and-Switch-Ports/Quality-of-Service/RDMA-over-Converged-Ethernet-RoCE/)

IB 则需要检查 Subnet Manager、Fabric 路由、分区以及相应的拥塞管理。NCCL 最终选择了哪张网卡、GPU Direct RDMA 是否生效，也要通过运行结果确认。Rail 接线不会自动替通信库完成这些工作。[NCCL 多网卡设置](https://docs.nvidia.com/deeplearning/nccl/archives/nccl_2303/user-guide/docs/env.html)

验收时，从单链路、单 SU、单 Pod 逐步测到跨 Pod，根据业务覆盖 AllReduce、All-to-All 等通信模式。还要断一条上联、停一台交换机，看看剩余容量、告警和作业恢复是否符合预期。

正常时恰好 1∶1，断掉一部分上联后，带宽就会减少。替代路径能绕过故障，不会补回失去的容量。

准备做第一张端口表时，可以把当前交付规模、最终规模、跨 Pod 的带宽要求写在表头。然后给每一条现有链路和未来链路标出两端位置。

**等到新增第十一个 Pod 时，它的线接到哪里，应该在第一期的设计里就能找到答案。**
